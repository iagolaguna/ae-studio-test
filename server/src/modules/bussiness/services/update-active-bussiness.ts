import { logger } from '~/common/logger'

export const findAndStoreData = async () => {
  logger.info('Updating active bussiness')

  // request data from third part api
  // parse data object properties
  // parse data values
  // save to db
}
